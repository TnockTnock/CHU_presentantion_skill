# Проверка переносимого скилла — 2026-09-24

- Python stdlib: все 12 типов слайдов и 6 вариантов текстовых блоков собираются без Codex runtime; 30 локальных тестов проходят.
- Изолированная распаковка ZIP: `python3 -S` собирает демо из 7 слайдов. Сторонние Python-пакеты не загружаются.
- macOS / bundled LibreOffice: просмотрены 21 слайд из demo, library-demo, text-blocks-demo и edge-cases. Проверены таблицы, графики, схемы, кириллица, Golos Text, SemiBold заголовков и Bold чисел.
- Отрицательные столбцы требуют явного invertIfNegative=0; линейные графики используют smooth=0. Исправление проверено повторным рендером и закреплено тестом.
- Каталогизация PDF: 3 страницы, 0 ошибок; требует pypdf/Pillow и рендереры.
- Прежний Codex backend: демо из 7 слайдов проходит структурную проверку.
- Установщик: проверены 6 профилей, отказ от перезаписи, резервная копия и сохранение local/. ZIP исключает личные материалы.
- GitHub Actions содержит матрицу Linux/Python 3.9, Windows/Python 3.12 и macOS/Python 3.12. Результаты CI следует смотреть в Actions.

Не проверены запуск каждого клиентского приложения, все модели, визуальный рендер на Windows/Linux и открытие вручную в Microsoft PowerPoint. Универсальность означает отсутствие зависимости сборщика от API модели; выполнение команд и Python нужны в среде агента.
